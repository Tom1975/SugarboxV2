[pytest]
# test_step.py is a standalone script (not a pytest suite) — not collected.
# test_conformance.py lives in z80-debug-adapter/ (run explicitly in CI).
python_files = test_protocol.py
testpaths = .
